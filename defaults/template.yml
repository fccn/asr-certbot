---
contact_email: 

acme_accounts: 
  - name: 
    ca:
    server: 
    mac_id: 
    # mac_id: "{{ vault_service_mac_id }}"
    mac_key: 
    # mac_key: "{{ vault_service_mac_key }}"

# acme_accounts: 
  # - name: "SERVICE NAME"
    # ca: #acme-v02.harica.gr  
    # server: #https://acme-v02.harica.gr/acme/XXXXXXXXXXXXXXXXXXXXX
    # mac_id: "{{ vault_service_mac_id }}"
    # mac_key: "{{ vault_service_mac_key }}"